{
  "private": true,
  "scripts": {
    "gc:init": "gcloud init",
    "gc:login": "gcloud auth application-default login",
    "gc:logout": "gcloud auth application-default revoke && gcloud auth revoke",
    "init": "terraform init",
    "plan:dev": "npm run init && terraform plan -var-file=development.tfvars",
    "apply:dev": "npm run init && terraform apply -var-file=development.tfvars"
  }
}
