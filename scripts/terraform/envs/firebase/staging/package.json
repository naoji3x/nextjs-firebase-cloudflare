{
  "private": true,
  "scripts": {
    "gc:init": "gcloud init",
    "gc:login": "gcloud auth application-default login",
    "gc:logout": "gcloud auth application-default revoke && gcloud auth revoke",
    "init": "terraform init",
    "plan": "npm run init && terraform plan",
    "apply": "npm run init && terraform apply"
  }
}
