{
  "private": true,
  "scripts": {
    "init": "terraform init",
    "env": "node env-build.js",
    "plan": "npm run init && terraform plan -var-file=development.tfvars -var-file=env.tfvars",
    "apply": "npm run init && terraform apply -var-file=development.tfvars -var-file=env.tfvars"
  }
}
